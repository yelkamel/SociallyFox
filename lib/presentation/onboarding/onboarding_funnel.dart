import 'package:flutter/material.dart';

class OnboardingFunnel extends StatelessWidget {
  const OnboardingFunnel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Onboarding funnel'),
      ),
    );
  }
}
